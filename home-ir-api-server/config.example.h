#pragma once

// Copy this file to config.h only if board-specific values are needed.
// Wi-Fi credentials are intentionally not stored here; use NVS provisioning.

#define HOME_IR_TX_GPIO 4
#define HOME_IR_RX_GPIO 5
#define HOME_IR_HTTP_PORT 80
#define HOME_IR_HOSTNAME "home-ir-api-server"
